# 🧩 ZAI Core Architecture Overview

**ZAI Core** is designed to be modular and extensible.  
Every function is built to be lightweight, easy to understand, and reusable across multiple AI systems.

## Components
- **core.py** → Brain system
- **data_handler.py** → Handles data cleaning & preprocessing
- **utils/** → Extra helper modules
- **tests/** → Unit tests (planned)

## Vision
ZAI Core will act as a personal AI framework capable of connecting various AI agents and projects.
